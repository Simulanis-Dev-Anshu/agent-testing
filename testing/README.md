# UI Component Playground

Static HTML/CSS/JS page for testing the AI review bot on frontend changes.

## Files

- `index.html` — layout, cards, table, form, modal
- `styles.css` — dark theme component styles
- `app.js` — tab switching, modal open/close, form submit handler

Open `index.html` in a browser to preview the UI before raising a PR.
